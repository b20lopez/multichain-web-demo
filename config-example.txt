default.name=Default                   # name to display in the web interface
default.rpchost=10.140.0.3              # IP address of MultiChain node
default.rpcport=4330                  # see rpc-port from chain parameters
default.rpcuser=multichainrpc          # username for RPC from multichain.conf
default.rpcpassword=8kDzEpq7ot3oF5nidJD1czx223nv15JL94Sf7jx5uUxs                  # password for RPC from multichain.conf

# You can configure support for other nodes using a word other than 'default':
#
# another.name=...
# another.rpchost=...
# ...etc...
